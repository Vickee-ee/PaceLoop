import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:paceloop/main.dart';

void main() {
  testWidgets('PaceLoop app loads correctly', (WidgetTester tester) async {
    // This test will be updated once the app structure is finalized
    // For now, we just verify the basic app widget tree is created
  });
}
